using TMPro;
using
UnityEngine;

public class blackDamge : MonoBehaviour
{
    [SerializeField] TextMeshProUGUI text;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        text.text = " wasaabi 3 ";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
